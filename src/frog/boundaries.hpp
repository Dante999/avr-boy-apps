#ifndef BOUNDARIES_HPP
#define BOUNDARIES_HPP

#include <cstdint>

struct boundaries_s {
    uint8_t min_x = 0;
    uint8_t max_x = 0;
    uint8_t min_y = 0;
    uint8_t max_y = 0;
};

#endif // BOUNDARIES_HPP
